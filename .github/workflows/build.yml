name: CI/CD Action Test

on:
  pull_request:
    branches:
      - dev

jobs:
  test:
    runs-on: windows-2019
    container:
      image: node:20
    steps:
      - uses: actions/checkout@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build


